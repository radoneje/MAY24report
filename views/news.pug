.news
    each n in news
        .newsItem.mb-2(isArchive=n.isArchive newsid=n.id)
            .newsItemTitle #{n.Name}
            .newsItemDate #{n.dt}
            .newsButton.mt-1
                input(type="button" value="Скрипт")
